version: '3.1'

services:
  app-tch:
    build: .
    restart: always
    ports:
      - "8001:8000"
    environment:
      - SITE=TCH
  app-methodist:
    build: .
    restart: always
    ports:
      - "8002:8000"
    environment:
      - SITE=METHODIST
  app-bslmc:
    build: .
    restart: always
    ports:
      - "8003:8000"
    environment:
      - SITE=BSLMC
  app-harrishealth:
    build: .
    restart: always
    ports:
      - "8004:8000"
    environment:
      - SITE=HarrisHealth
  app-houston_va:
    build: .
    restart: always
    ports:
      - "8005:8000"
    environment:
      - SITE=HoustonVA
  app-national_va:
    build: .
    restart: always
    ports:
      - "8006:8000"
    environment:
      - SITE=NationalVA